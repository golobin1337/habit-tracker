{
  "buildCommand": "cd habit-tracker && npm install && npm run build",
  "outputDirectory": "habit-tracker/dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
